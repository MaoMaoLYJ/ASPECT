# WS

Parameter-efficient training with workflow-level sharing.

Run `python -m algorithms.WS --help` for the portable CLI.
The method's checked-in defaults are in `config.json`. The common trainer
preserves the same data, reward, GRPO update, and diagnostic implementation
across methods. See the root README and `docs/METHODS.md` for semantics.

